# AI 助手配置接入约定

本项目的 AI 助手应能读取和操作所有面向用户的游戏配置。新增、修改或移除配置功能时，必须在同一变更中同步维护 AI 助手，不能只更新页面。

- 维护 `src/engine/assistantConfiguration.ts` 中的 `assistantResources`。这是实际发送给助手的能力档案，必须包含资源说明、字段 schema、读取、业务校验、保存和即时界面同步。
- 同步维护 `docs/ai-assistant.md` 的能力范围、限制、示例和兼容策略。新增独立配置域必须注册资源，不得声称助手自动理解未接入的存储。
- 新增现有配置字段时，确保局部 JSON Patch 可读取和修改它；为枚举、范围、引用、迁移等约束补充 schema 或业务校验。校验不得通过丢弃未知字段来破坏新版本数据。
- 保存必须保留未修改字段、历史回合、凭证与其他存档的未保存进度。旧助手动作和 `model_assistant_backend` / `model_assistant_model` 偏好键必须继续兼容，或提供显式迁移。
- 新增角色/世界定义时复用对应领域的校验器与迁移逻辑，定义和数据需要一起保持有效。不能绕过玩家存在等世界不变量。
- 凭证不得出现在模型上下文、普通配置 JSON 或执行回执中；凭证管理继续使用既有安全表单/接口。日志、历史回合、运行时任务状态不能伪装为可写配置。
- 对配置变更补充有意义的助手测试：读写和落盘、未知字段保留、非法数据与引用拒绝、取消、冲突和失败回执；在 CSP 禁止动态代码生成的条件下运行。
- 更新 UI 功能时检查助手入口仍在“历史”下方、名称为“AI 助手”，并保持独立 Backend/模型选择。

这些规则不要求额外用户确认；按用户已授权的任务正常完成接入。

# 测试分级与最小干扰约定

## 临时文件目录

- 日志、命令输出、调试截图和临时测试数据统一存放在项目根目录的 `.tmp/` 下，不得写到根目录。
- 日志放入 `.tmp/logs/`；其他临时文件按任务建立 `.tmp/<任务名>/` 子目录。写入前创建所需目录，例如 `New-Item -ItemType Directory -Force .tmp/logs`，测试日志重定向示例：`npm run check:csp *> .tmp/logs/check-csp.log`。
- `.tmp/` 已加入 `.gitignore`，不要提交其中的临时产物。需要长期保留的报告或测试夹具应整理后放入对应的文档或测试目录。
- 清理时只删除已确认属于任务的临时文件；不得清理用户的真实配置、存档或正在使用的文件。

按改动涉及的能力选择最低足够的测试级别；只有低级别无法验证目标行为时才升级，不要求每次逐级跑完。游戏逻辑优先直接调用内部 TypeScript 模块，不默认启动 exe。必须区分 mock 验证、真实模型验证、原生集成验证和界面验证，不能以其中一种通过代替其他种的结论。

| 级别 | 验证内容 | 使用方式 | 是否启动游戏窗口 |
| --- | --- | --- | --- |
| L0 静态与构建检查 | 类型、模块引用、前端构建；涉及打包时检查打包产物 | `npm run typecheck`；需要时 `npm run build`，原生打包用 `npm run build:exe` | 否；构建成功不代表运行验收通过 |
| L1 模块与规则测试 | schema、JSON Patch、世界不变量、迁移、配置与助手读写、非法引用、取消和冲突等 | `tests/**/*.test.ts` 中的相关用例；可用 `node --disallow-code-generation-from-strings --import tsx --test tests/<文件名>.test.ts` 定向运行，完整 CSP 回归用 `npm run check:csp`；普通全集为 `npm test` | 否 |
| L2 无界面集成测试 | 完整回合管线、NPC 行为、状态结算、重试、历史分支、失败恢复及存储适配逻辑 | 直接调用 `GamePipeline.executeTurn()` 或相关 store/服务，以隔离数据及 mock/stub 传输验证；使用现有集成测试，回合冒烟可用 `npm run eval:narrator -- --mock`；Rust 侧用 `npm run check:rust` | 否；mock 存储不能证明原生落盘正确 |
| L3 真实模型评测 | 实际提示词、模型响应、叙事与行为质量、多阶段模型协作 | 使用 `npm run eval:narrator` 或与任务相关的 `eval:*` 脚本，直接调用内部管线；使用已授权的模型连接和隔离测试数据，限制场景数、重试和并发 | 否；会发起真实模型请求，可能产生费用 |
| L4 原生集成测试 | Tauri 通信、原生网络、SQLite/文件落盘、重启恢复、原生流式响应 | 仅在需要时构建启用 `test-control` feature 的测试程序；设置 `STORY_TAVERN_TEST_PORT`、至少 24 字符的 `STORY_TAVERN_TEST_TOKEN` 和独立的 `STORY_TAVERN_TEST_DIR`，通过本机 `POST /command` 控制接口及 `scripts/native-playtest.mjs` 等脚本验证 | 需要运行测试程序，但优先后台、隐藏或不激活窗口，通过 API 操作 |
| L5 界面与发布验收 | 布局、点击交互、焦点、滚动、可见状态、窗口行为及正式包运行 | Web 界面优先独立无头浏览器；仅原生窗口或正式包特有问题才启动对应 exe 做最少量可见操作。UI 更新须检查“历史”下方的“AI 助手”入口及独立 Backend/模型选择 | 仅必要时显示；正式包不开放测试控制接口 |

## 执行约束

- 测试进程、浏览器会话、端口和数据目录必须与用户正在使用的游戏及浏览器隔离；不得操作用户当前游戏实例，不得覆盖真实存档、配置或未保存进度。凭证只走既有安全接口，不写入报告或普通测试数据。
- 在启动现有评测脚本前检查其行为，不凭脚本名称假设是 mock 或无副作用；例如原生试玩脚本可能自动切换真实模型、创建存档和保存，只能连接隔离测试实例。
- 默认不抢焦点、不最大化窗口、不移动用户鼠标、不发送全局按键、不占用剪贴板、不打开无关页面。优先内部 API、测试控制接口和无头浏览器；不得为可由这些方式完成的检查操作桌面。
- Windows 后台启动辅助进程时使用 `Start-Process -WindowStyle Hidden`；若程序自身仍弹出或激活窗口，不把该参数视为已实现隐藏，应减少启动次数，并改用可用的后台测试路径。确需可见验收时先简短说明原因和范围，按已有任务授权执行，不重复索要确认。
- 控制 CPU、内存、模型请求并发和日志量，避免重复全量构建或并行启动多个游戏实例；相关检查通过后，只有新增改动、失败或未解决的风险才扩大或重复测试。
- 记录本次启动的 PID、端口和临时目录，结束后只清理本次创建的进程与资源；不得按进程名批量关闭用户程序。删除临时目录前核实绝对路径位于本次测试目录内。
- 汇报实际执行的级别、命令、结果及未覆盖的关键行为。未启动 exe 时明确原生运行或可见界面尚未验证，不为补齐形式上的层级而打扰用户电脑。
- 仅修改文档时检查内容与差异即可，不为文档变更启动游戏、调用真实模型或运行整套测试。
