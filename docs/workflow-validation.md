# 组合管线切换验证

2026-09-13：游戏和独立文本任务统一到 runWorkflow；旧状态执行器与硬编码文本执行器退役。

## 自动检查

- L0：npm run build 通过，包含 TypeScript 检查。保留现有的 bundle 体积提示，不代表原生运行验收。
- L1/L2：npm run check:csp 通过，297 项测试。覆盖通用步骤引用、输入/输出校验、同 Agent 重用、实际 Runtime 配合 stub HTTP、默认故事适配器、人物创建/重新生成、历史、取消、提交失败、未知字段、配置库 CAS 和助手跨资源保存。
- 删除仅依赖退役状态管线的执行测试，保留纯领域验证、历史证据分析和存档兼容测试；新增 story_workflow.test.ts 覆盖游戏实际使用所选组合和原子保存。

## 对照与浏览器

- 固定响应回放：从变更前版本临时载入旧文本执行器，对比无人回应/一人回应两个场景。新旧调用上下文、渲染 Prompt、最终正文及人物 end_state 完全一致。旧执行器仅在 .tmp 验证脚本中出现，不随应用提交。
- L3/L5：隔离无头 Edge、独立本地 Rust 存储服务与数据库。在组合页面复制默认故事组合并选择它，游戏页面通过真实 Fast 组模型 deepseek/deepseek-v4.1-flash 完成一回合。
- 成功回合实际调用 route、outlines、narration 三步；cards 无创建需求而跳过。保存标记为 workflow-v1，包含所选 workflowId，revision 从 0 到 1；刷新浏览器后正文和 saved 状态一致。
- 检查“历史”下方 AI 助手入口及独立 Backend/模型选择通过。截图已检查。
- 两次早期浏览器尝试因测试脚本错误地等待异步谓词而过早读取并终止，仅各开始一次请求；修正为等待实际界面忙碌/完成后，通过上述完整回合。没有据此修改游戏逻辑或声称前两次完成。

## 覆盖边界

本次真实浏览器验证包含本地服务的网络代理、SQLite/Markdown 保存与刷新恢复；未启动 Tauri 游戏 exe，未重新验收正式包窗口或重启操作系统。mock/stub 与固定响应回放不证明真实模型逐字确定性；真实样例也不证明所有剧情语义正确。

测试数据、原始输出、截图与日志均在 .tmp/workflows 和 .tmp/logs；独立服务及浏览器已关闭。没有写入用户正式存档、模型配置或凭证。
